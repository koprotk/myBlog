# myBlog
My blog project, consist in the creation of my own post on org mode and then publish it with a small script in python, the title, the tags, is management automatically with the package orgparse. The export from org to html is doing by emacs and works amazingly well.
